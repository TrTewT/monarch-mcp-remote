# Serveur amont, epingle sur un commit precis : une branche mobile qui change
# sous les pieds d'un deploiement financier est exactement ce qu'on ne veut pas.
monarch-mcp-server @ git+https://github.com/robcerda/monarch-mcp-server.git@41715c1cb3c66c08068dfc7b4ddd0b9109036eac

# Le SDK MCP officiel. La 2.x expose streamable_http_app(), qui est l'API
# utilisee par remote_server.py.
mcp[cli]>=2.1,<3

uvicorn[standard]>=0.30
starlette>=0.37
